name: gradle-build
on:
  push:
    branches:
      - 'feature/*'
      - 'release/*'
      - 'fix/*'
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  gradle:
    uses: josephrodriguez/github-workflow/.github/workflows/java-gradle-build.yaml@7056cf1ec83b1b5e4a0ca9c7e19da89d1c882c08
    with:
      cache_gradle: true
      cache_sonar: true
      java_version: '25'
      java_distribution: 'temurin'
      gradle_enable_scan: true
      gradle_enable_sonar: true
      gradle_log_level: info
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  dependabot:
    needs: [gradle]
    permissions: write-all
    uses: josephrodriguez/github-workflow/.github/workflows/dependabot-merge-pull-request.yaml@7b5e97ad78ba7232ccfb92d8b50c1ee02517bdbc
    secrets:
      AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}      
